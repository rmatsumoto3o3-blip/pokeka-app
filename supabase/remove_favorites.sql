-- ============================================
-- お気に入り機能の削除（オプション）
-- ============================================
-- お気に入りテーブルとビューを削除します
-- 注意: このSQLは実行しなくても問題ありません
-- データベースに残っていても害はありません

-- ビューの削除
DROP VIEW IF EXISTS public.reference_decks_with_favorites;

-- お気に入りテーブルの削除
DROP TABLE IF EXISTS public.reference_deck_favorites;

-- ============================================
-- 削除完了
-- ============================================
-- お気に入り機能が完全に削除されました
